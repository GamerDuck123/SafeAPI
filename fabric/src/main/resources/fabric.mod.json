{
  "schemaVersion": 1,
  "id": "safeapi",
  "version": "${version}",
  "name": "${name}",
  "description": "${description}",
  "authors": [
    "GamerDuck123"
  ],
  "contact": {
    "homepage": "${website}",
    "sources": "${sources}",
    "issues": "${issues}"
  },
  "license": "CC0-1.0",
  "icon": "assets/safeapi/icon.png",
  "environment": "*",
  "entrypoints": {
    "main": ["${group}.SafeAPI"]
  },
  "mixins": [],
  "depends": {
    "fabricloader": ">=${fabricLoaderVersion}",
    "minecraft": "~${minecraftVersion}",
    "java": ">=17",
    "fabric-api": ">=${fabricApiVersion}"
  }
}